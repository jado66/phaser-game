{
  "name": "phaser-game",
  "version": "1.0.0",
  "main": "dist/index.html",
  "scripts": {
    "start": "esbuild src/index.ts --bundle --outfile=dist/index.js --servedir=dist --watch", 
    "build": "esbuild src/index.ts --bundle --minify --outfile=dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "esbuild": "^0.21.5",
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "@babel/core": "7.2.0"
  }
}
